# Phase 0 Baseline — 2026-07-27

## Source

- Product: CareerPilot CN
- Upstream: `santifer/career-ops`
- Upstream version: `1.22.0`
- Upstream commit: `2901944`
- Fork repository: `gaosanliukoushui/career-pilot-cn`
- Upstream policy: fetch-only; automatic `apply` is disabled

## Windows environment

- Node.js: `v24.14.0`
- npm: `11.9.0`
- Playwright: `1.62.0`
- LibreOffice: `26.2.4.2`
- LibreOffice CLI: `E:\liberoffice\program\soffice.com`

## Installation and onboarding

- Root dependency installation completed; Playwright Chromium and headless shell were installed.
- Web `npm ci` completed with 3 high-severity audit findings. No destructive `npm audit fix --force` was applied.
- Doctor correctly reported onboarding required because `cv.md`, `config/profile.yml`, and `portals.yml` are absent in a clean checkout.
- Doctor created ignored customization seeds and warned that Playwright MCP tools were not detected; neither condition blocks deterministic document generation.

## Baseline verification

- Web typecheck: passed.
- Web production build: passed, with one existing Turbopack file-tracing warning caused by dynamic repository-root access.
- Core suite before CareerPilot CN feature work: 2062 passed, 8 failed, 2 warnings.
- Two failures came from the initial engineering-skill configuration placing project rules in `CLAUDE.md`; Phase 0 moves those rules to the canonical `AGENTS.md` and restores the thin wrapper.
- One failure is an upstream Playwright SSRF redirect mock incompatibility (`controls.some is not a function`).
- Five failures are upstream Windows batch-runner model-routing fixtures that do not capture the spawned argv.

These upstream failures are baseline evidence, not accepted final-state regressions. CareerPilot CN tests added in Phase 1–2 must pass, and the complete suite is rerun before release with baseline failures classified explicitly.

## Data boundary

Real candidate data, evidence, migration backups, manifests, and generated resume variants live under the ignored user layer. Only Schemas, policies, templates, code, and anonymous fixtures may be tracked.
